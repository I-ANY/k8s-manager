---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: appconfigs.operation.operation.top
spec:
  group: operation.operation.top
  names:
    kind: AppConfig
    listKind: AppConfigList
    plural: appconfigs
    singular: appconfig
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AppConfig is the Schema for the appconfigs API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of AppConfig
            properties:
              appName:
                description: 应用名称，用于 Deployment / Pod / Label 等
                minLength: 1
                type: string
              enableMetrics:
                description: 是否开启 metrics（比如决定是否加 sidecar、port 等）
                type: boolean
              env:
                description: 环境变量（name/value 列表）
                items:
                  description: EnvVarKV 用于表示一个 name/value 形式的环境变量
                  properties:
                    name:
                      description: 环境变量名
                      minLength: 1
                      type: string
                    value:
                      description: |-
                        环境变量值
                        如果你希望允许空字符串，可以去掉 MinLength 这行
                      minLength: 1
                      type: string
                  required:
                  - name
                  - value
                  type: object
                type: array
              image:
                description: 应用镜像
                minLength: 1
                type: string
              replicas:
                description: 副本数，可选；如果为空，controller 里默认 1
                format: int32
                type: integer
              strategy:
                description: 部署策略，比如 RollingUpdate / Recreate
                enum:
                - RollingUpdate
                - Recreate
                type: string
            required:
            - appName
            - image
            type: object
          status:
            description: status defines the observed state of AppConfig
            properties:
              conditions:
                description: |-
                  标准 Conditions，方便 kubectl / 控制器生态复用
                  使用 listType=map + listMapKey=type 来支持 patch 合并
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastUpdateTime:
                description: 最近一次状态更新的时间
                format: date-time
                type: string
              message:
                description: 最近一次状态变化的说明信息
                type: string
              phase:
                description: 当前阶段：Progressing / Available / Failed 等
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
